{
  "name": "@bettr/server",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon --watch src --watch ../client/react src/index-dev",
    "build": "rspack build",
    "prod": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}